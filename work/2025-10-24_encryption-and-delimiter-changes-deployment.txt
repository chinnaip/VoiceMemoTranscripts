I'm working on this with a tight deadline, but I did get enough testing where I'm very comfortable with it. So the three changes that I made are: the PGP field encryption, using multi-character delimiter to avoid broken data points, and also removed customers from ended SDIDs to share no appearance at the 2271 error code. That's the one that we had to make a mass deletion of recently. So I was able to get that fixed in as well. I have the pull request just for diff purposes. I know you don't have to approve it, but I just want you to see that I'm not making any changes that I shouldn't be making. Obviously, changes to the config file go from a config JSON structure to an encrypted format. This is just on a database. I mean, you should encrypt all the versions of it: production, etc. So that's the encrypted file. I'm running straight into the script. There's only a few changes, right? So instead of just opening up the file straight up as the configuration file, we use the Fernet key and decrypt the contents and then load that JSON string. And that becomes our config. Then I changed all the delimiters from backslash T to caret percentage sign, just literally the keys that are next to each other. Don't judge me. This is debugging, so these are just comments. Then that's just debugging. Don't worry about that. Changed the limiter, changed the delimiter, changed the liter multiple times. And then here, where we have the inactive accounts with FDID end date, I just remove all of the logic. I just do continue so that we don't do any of the add customer functions or anything like that. So those are the three changes. Most importantly, I was able to actually put all the stuff into JS app to queue it. And when it ran, it actually gave me this list of mnemonics, which is why I sent them to you. These are the mnemonics that are reported to CAT, but we're not reported to CASE. I still see HTP bids, HTPD liquid. I know we talked about that a long time ago. I forgot what it was, but if these are accounts that are actively trading in CAT frequently and we're not submitting any CASE report for them, and this is the full superset of all mnemonics that we can possibly report, then these would still be a gap, right? And Finra could always reach out to us like, hey, why is it that you're actively trading on these mnemonics and we have nothing? The last bits: it's still confidence. I did submit it. Out of a sample of 5,668 SDIDs, only four rejected. Out of 5,571 customers, only three rejected. Out of those seven total rejections, four of them are related to the four U.S. mnemonic and the 4W1 mnemonic. It looks like that still hasn't been updated. I need to check that. My connection was breaking as I was checking that. So give me five minutes to get back to you on whether that was updated or not. But yeah, a total of seven rejections on the full sample.