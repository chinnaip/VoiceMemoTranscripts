We do a lot of tests for a single order, like testing the complete system to see how it's capturing data and what results it's producing. Then we compare that to the original submission to understand if we're missing anything and where we're going wrong. In fact, I was asked yesterday to start discussions with FIs on how they update values and handle error correction when something goes wrong.

So what is normalisation? By normalisation, I mean formatting and reformatting data—data cleaning, right? When we're getting data from different sources, everything will be in different formats, different styles, different headers. We have to ensure that everything is consistent so we can use it in a better way and establish unique keys.

What I'm hearing from that is that normalisation—data transformation where you're trying to convert the PCTC data into CAT data—that's not actually normalisation, right? Normalisation would be, for example, when you want to do a recon between the CAT data and the PCC data. You have to normalise these two fields, map them. For example, the CAT records might have a sample in a particular format and the PPC data might have another format. You're doing a comparison between two existing data sets. Then you're transforming both of them into one single format—one single date format.

So instead of calling it normalisation, I would say that's designing a dataset, right? Because what you're doing is designing a third data format that fits the two that you have. That's the design of how we should consolidate these two datasets. This is what's always talked about—we don't appropriately design our datasets. We're missing standardised data design. I think there's something missing there, right?