Oh, I see. That makes sense, yeah. We can send you the conversation then, and if anybody jumps in, we'll be ready. So for me, I just want to target the easy ones first and see how much value we get, then show it and try to extract some value. Eventually, we can use the practices we build and the processes we develop around that for any models we propose, which would be based on generative AI models, right? Either LLMs or anything like that, building agents and that kind of thing.

Nowadays, what I see is that once you have some sort of semantic models like what we build—we build a sort of graph—there are two ways we can use it. One is trying to apply some models to predict and even generate the fix for it. All these things we can do. That's one side of it. Another way is if we integrate that graph into some sort of RAG, right? Retrieval-Augmented Generation with graph stores. If you put it there, the agent can use it, right? So business can say, okay, give me the whole trail of this particular order, just in natural language, and they can interact with that.

Once we build these graph-kind models, that's usable in two scenarios: the agentic chat way of using it, plus the actual models running to generate something, predict something. Both of these ways work. And then it's not always necessary to be an LLM. We can have some other powerful models that we can run locally. It doesn't cost that much like LLMs do, you know, to do a lot of these predictions, right? So it's much more powerful to identify, especially these intra-form linkage errors and then even detect what should have been there which is missing. It will clearly tell us, right?

So I probably will come up with a complete research report on that, right? With complete details, data, and stuff. The current data we have, what kind of variables we need to build, and those things, I can come up with that. But before that, as I said, if we have some simple model that we are able to run and show, you know, how proactively we are able to tag them well, and then if we're able to show from the UI that it'll take little effort to go to UI integration, but at least we can show from the backend perspective that this is the reason I flagged this as this error, and these are the two values that caused it. That saves a lot of time. For example, whoever wants to correct the particular record doesn't need to do a lot of root cause analysis. They say, okay, yeah, it's these two values because of this data flaw—that flaw is an error. Either so the system picks it up, or if I know, oh yeah, I know this particular field always has a problem, then I'll run my automation, right? It narrows down the whole root cause analysis spot for me.