Class, recording in progress. It is becoming highly impossible to handle data in traditional ways. For example, in the field of data science and AI, we're talking about billions of data points being collected regularly for pattern recognition or machine learning tasks. In that scenario, you can't even load those data points to your computer terminal and start analyzing them using spreadsheets like we did earlier. How do we deal with such large volumes of data? Deep learning models are able to do that. They process the data and generate actionable intelligence in the form of visual representation for you. Depending on the visualization patterns generated—the base or reference visualization patterns—and the new pattern that the deep learning model outputs after processing the data automatically, if there is a change in the pattern, you can use your domain knowledge or expertise to decide what needs to be done. So these humanly impossible tasks, deep learning models are now able to handle them. This isn't intellectually challenging, but because of the sheer volume of data, it's impossible to deal with such data manually. That's what I mean by humanly impossible tasks. Deep learning models are able to accomplish these tasks. These are the capabilities we're now enjoying in the field of deep learning. All of this became reality because of the ability to train deep networks. This is what was dreamed in the late 1990s when advanced neural network models were proposed—what we later called deep learning or deep networks. People visualized that we should be able to do all these things in class. But they struggled to train the network, so the whole interest went down. Now, what was that challenge in training a deep learning model? There is a well-known, very famous problem associated with deep learning model training called the vanishing or exploding gradient problem. This was the main hidden obstacle for enabling and training deep learning models. I think Sunil sir will explain this in detail in the next session, but I'll just provide an introduction. What happens is, if you recall what we discussed earlier in the perceptron material, there are three perceptron layers. Just for imagination, suppose there's an input layer. After that, there is a first hidden layer, which I'll call hidden layer one. The next hidden layer is hidden layer two. And the next hidden layer is hidden layer three.