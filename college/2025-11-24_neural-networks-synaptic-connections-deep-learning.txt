You are using or infrequently using certain connections. Based on that, the awakening or strengthening of that connection happens. The activity at these synaptic junctions is how knowledge is stored in biological neurons. The same thing has been mimicked in artificial neurons as what I call weight parameters. With increasingly large amounts of data, we learn the parameters representing these junction strengths.

Accordingly, to mimic that, each neuron performs a simple task: it receives inputs, sums them, compares with the bias, and once it exits the bias, we say the neuron is fired. The fired output is nonlinear. That's somewhat of a value. The value itself minus the threshold is called the activation value. That activation value passes through a nonlinear output function, which is the output of the neuron. If you quickly observe the structure in which it is doing this, it's just a summation compared with the threshold. Then that value passes through a nonlinear output function. It's a very simple activity compared to computing, processor action. If you take a CPU, you can implement a very complex mathematical function there to execute. But in a neuron, we are talking about such simple activity. Then where is the power of the neural network or deep learning model coming from?

Again, it is inspired by the structure and organization. Structure means the organization of neurons. We have several billions of neurons organized in layers. The strength or efficiency of this neural network or deep learning models in terms of modeling information comes because of the layered networks of these neurons in the form of layers, and several such layers are there. Only then do they become very effective. Otherwise, if you just take a function of a simple biological neuron, a simple artificial neuron by itself doesn't have that capability. But the real strength comes once you make them as layers of interconnected neurons, which we call neural networks. That is the real power of neural networks or deep learning—when you arrange all of them in layers. That is how it happens in the human brain. Several billions of neurons arranged in layers. Information flow happens from the base to the learning centers in the highest part of the brain. That is how we are able to do that. And that motivates the organization of these artificial neurons in a layered manner.

Of course, there are some aspects called activation dynamics and synaptic dynamics. You don't have to bother much, but if you want, you can read about it in any neuroscience text. Activation dynamics means the activation value of the neuron. There is a difference between the activation value and the actual output.